1、获得鼠标的坐标值 他不受页面大小的影响 而是在可视区域内的坐标值(x,y)
<body>

    <script>
        document.addEventListener("click", function(event) {
              // 1、可视区域
            console.log(event.clientX);
            console.log(event.clientY);
            console.log("================");
              // 2、整个页面的XY值 
            console.log(event.pageX);
            console.log(event.pageY);
        })
    </script>
</body>
